{
  "name": "ann-liu.github.io",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel src/index.html --no-cache",
    "build": "npm run clean && parcel build src/index.html --no-source-maps --public-url '.'",
    "clean": "rm -rf dist"
  },
  "author": "ann liu",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-beta.3.1",
    "parcel": "^2.0.0-beta.3.1",
    "sass": "^1.34.0",
    "typescript": "^4.3.2"
  },
  "prettier": {
    "arrowParens": "avoid",
    "singleQuote": true
  }
}
